let text = '나는 토마토다';

console.log(text.length);
console.log(text[3], text[4], text[3]);

// '문자열'은 배열이다. (== 순서가 있다.)
// ==> 문자 '열'이 배열의 '열'이다.
// ==> 문자 형태의 배열이다. 따라서, '배열접근이 가능하다.'

// '문자'는 char (charactor) : 말 그대로의 '문자'